// frontend/src/app/models/enums.ts

export enum ContentType {
  Image = 'image',
  Audio = 'audio',
  File = 'file'
}

export enum EntryStatus {
  Processing = 'processing',
  Ready = 'ready',
  Error = 'error'
}